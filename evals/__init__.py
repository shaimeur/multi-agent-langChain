"""Evaluation harnesses and benchmark data — not part of the shipped `forge` package.

Kept importable (rather than a loose pile of scripts) because D8's `swe_mini` is
library code two callers share: `run_swe_mini.py` and `tests/test_swe_mini.py`.
"""
