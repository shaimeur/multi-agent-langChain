"""Retrieval evaluation — the §13.1 metric set and the ablation harness.

Library code, so the ``./evals/*.py`` scripts and the test suite import the same
scorer instead of each carrying their own. The scripts stay thin runners; the
numbers they print come from here.
"""
